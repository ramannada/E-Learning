<div class="user-panel">
    <div class="pull-left image">
        <img src="{{base_url()}}/upload/images/{{login.data.photo}}" class="img-circle" alt="User Image" height="200px">
    </div>
    <div class="pull-left info">
        <p>Hi, {{login.data.name}}</p>
    </div>
</div>

<ul class="sidebar-menu">
    <li class="header">
        <a href="{{path_for('web.home')}}">Home</a>
    </li>
    <li class="treeview">
        <a href="#">
            <i class="fa fa-graduation-cap"></i> <span>Courses</span>
        </a>
    </li>
    <li class="treeview">
        <a href="{{path_for('web.get.my.article')}}">
            <i class="fa fa-comment-o"></i> <span>Article</span>
        </a>
    </li>
</ul>
